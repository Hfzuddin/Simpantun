# Requires Python 3.10
#
# Direct dependencies only — pip resolves the rest. The previous version of
# this file was a `pip freeze` of a whole dev environment and dragged in
# gradio, streamlit, fastapi, datasets and their trees, none of which this
# app imports. They cost image size and cold-start time on Cloud Run.

# CPU-only wheels: the default PyPI torch build pulls in ~2GB of unused
# NVIDIA/CUDA packages even though this app runs with gpu=False everywhere.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.3.0+cpu
torchvision==0.18.0+cpu

# OCR. Pulls opencv, scikit-image, scipy, shapely, pyclipper itself.
easyocr==1.7.1
opencv-python-headless==4.9.0.80

# Embeddings. The transformers/tokenizers/huggingface-hub pins are the set
# this app is known to run on; sentence-transformers 2.7 is sensitive to
# huggingface-hub API changes, so keep these four in step.
sentence-transformers==2.7.0
transformers==4.40.0
tokenizers==0.19.1
huggingface-hub==0.36.0

# Vector search
faiss-cpu==1.8.0

# numpy stays on 1.x: numpy 2 breaks the faiss and torch builds pinned above.
numpy==1.26.2
pandas==2.1.4
openpyxl==3.1.2
Pillow==10.1.0

# Web layer
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.1.3
gunicorn==23.0.0
